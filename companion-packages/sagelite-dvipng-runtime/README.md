# sagelite-dvipng-runtime

Optional dvipng executable companion package for sagelite wheels.

This package contains the `dvipng` command line program used by SageMath's
LaTeX rendering features. It is intended to be installed alongside the
`sagelite` wheel when these optional computations or doctests are needed.

This package does not provide a complete TeX distribution.
