# sagelite-singular-runtime

Optional Singular runtime data companion package for `sagelite`.

This package bundles Singular library data and dynamic Singular modules used
by Sage's libSingular interface. Install it alongside `sagelite` when doctests
or code need Singular library functions that are not built into the linked
shared libraries.
