# sagelite-singular-runtime

Optional Singular runtime data companion package for `sagelite`.

This package bundles Singular library data, Factory finite-field tables, and
dynamic Singular modules used by Sage's libSingular interface. Install it
alongside `sagelite` when doctests or code need Singular library functions or
Factory extension-field arithmetic data that are not built into the linked
shared libraries.
