# sagelite-database-mutation-class

Optional mutation class database companion package for `sagelite`.

This package contains the `database_mutation_class` data from SageMath 10.9,
installed as a Python wheel.  Installing it makes Sage's
`database_mutation_class` optional doctests and cluster algebra quiver mutation
class lookups available without making the data a hard dependency of
`sagelite`.
