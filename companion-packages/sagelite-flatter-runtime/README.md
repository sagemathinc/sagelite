# sagelite-flatter-runtime

Optional flatter executable companion package for sagelite wheels.

This package contains the `flatter` command line program used by SageMath for
some integer matrix LLL computations. It is intended to be installed alongside
the `sagelite` wheel when these optional computations or doctests are needed.
